# Generated test data

Run `make generate-test-pcaps`. Each Scenario A–G produces a `.pcap` and JSON oracle here. Files are deterministic for seed `20260720` and are intentionally not committed to keep the repository small.
